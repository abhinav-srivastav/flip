<html>
  <body>
  	Hi,<%= @user_name %>
    Congratulations, your order has been dispatched as on <%= @order.updated_at %>
    You will recieve it in shortly.
    Your order details:
  <% @order.line_items.each do |line| %>
  	Product Name : <%= line.varient.product.product %>
	  Quantity : <%= line.quantity %>
  	Amount : <%= line.price %>
  <% end %>
    Total amount: <%= @order.amount %>
    Dispatched at Address:
      <%= @order.address.name %>, 
      <%= @order.address.street_no %>,
      <%= @order.address.area %>, 
      <%= @order.address.city %>-<%= @order.address.postal_code %>     
    In case of any query, please mail us at customer_support@grab_ur_trolley.com
  </body>
</html>