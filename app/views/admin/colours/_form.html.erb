<%= simple_form_for ([:admin,@colour]), :html => { :class => 'form-horizontal'} do |f| %>
  <%= f.input :colour %>
  <%= f.submit :class => "btn btn-success"%>
  <%= link_to 'back', admin_colours_path, :class => 'btn btn-inverse'  %>
<% end %> 