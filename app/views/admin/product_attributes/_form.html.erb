<%= simple_form_for ([:admin,@attribute]), :html => { :class => 'form-horizontal'} do |f| %>
  <%= f.input :product_attributes %>
  <%= f.submit :class => "btn btn-success"%>
  <%= link_to 'back', request.referrer, :class => 'btn btn-inverse'  %>
<% end %> 